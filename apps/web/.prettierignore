# Build output and generated files. `next-env.d.ts` and `.next/types` are
# regenerated by `next typegen`, so formatting them would churn on every build.
.next
next-env.d.ts
node_modules
coverage

# pnpm owns this file's formatting.
pnpm-lock.yaml

# Generated from the API's OpenAPI schema by `pnpm gen:api-types`. Reformatting
# it would make the generator's output and the committed file differ, and the
# drift check compares them byte for byte.
lib/api-types.ts

# Playwright writes here on every run; the traces and the last-run record are
# not source.
test-results
playwright-report
