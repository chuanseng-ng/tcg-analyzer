[project]
name = "tcg-ml-corners"
version = "0.0.0"
description = "Corner condition assessment"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ml_corners"]
