[project]
name = "tcg-ml-condition"
version = "0.0.0"
description = "Neutral, company-agnostic condition representation"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ml_condition"]
