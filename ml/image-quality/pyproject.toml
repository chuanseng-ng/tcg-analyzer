[project]
name = "tcg-ml-image-quality"
version = "0.0.0"
description = "Image-quality gate: blur, glare, exposure, resolution"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ml_image_quality"]
