[project]
name = "tcg-ml-edges"
version = "0.0.0"
description = "Edge condition assessment"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ml_edges"]
