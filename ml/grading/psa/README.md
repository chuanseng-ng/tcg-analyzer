# `ml/grading/psa`

PSA grade distribution predictor.

Consumes the neutral condition representation and PSA's versioned grading rules.
Model bundles are versioned explicitly (`grading-psa-v0.2.0`), never
`/latest/`.

Populated in M8.
