[project]
name = "tcg-ml-grading-bgs"
version = "0.0.0"
description = "BGS grade distribution predictor"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ml_grading_bgs"]
