[project]
name = "tcg-ml-card-identification"
version = "0.0.0"
description = "Card identification against the canonical catalog"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ml_card_identification"]
