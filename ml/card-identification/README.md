# `ml/card-identification`

Matches a normalized card image against the canonical catalog.

A low-confidence match is never used silently — the user must confirm the
identification before analysis proceeds.

Populated in M9.
