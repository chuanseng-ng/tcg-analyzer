[project]
name = "tcg-analysis"
version = "0.0.0"
description = "Analysis pipeline orchestration and background inference"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_analysis"]
