[project]
name = "tcg-market-data-service"
version = "0.0.0"
description = "Serves pre-ingested market snapshots"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_market_data_service"]
