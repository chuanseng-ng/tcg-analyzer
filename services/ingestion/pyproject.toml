[project]
name = "tcg-ingestion"
version = "0.0.0"
description = "Scheduled ingestion of external market data"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_ingestion"]
