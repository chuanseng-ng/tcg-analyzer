# `tests/`

Repository-level tests that cut across workspace members.

Per-package unit tests live beside their package. `tests/` is for checks about
the repository as a whole — the spec §7 structure, the Compose stack, the
environment example, and the documentation.

Run with `uv run pytest`.
