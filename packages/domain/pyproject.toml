[project]
name = "tcg-domain"
version = "0.0.0"
description = "Framework-free core domain types for TCG Grading Advisor"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tcg_domain"]
