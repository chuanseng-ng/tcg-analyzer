# `packages/market-data`

The `MarketDataProvider` port, response normalization and price-confidence
modelling.

No provider may become a hard dependency of the core domain. Do not assume or
hard-code TCGplayer API access.

Populated in M4 (#49).
