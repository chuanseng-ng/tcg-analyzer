# `packages/shared`

Cross-cutting utilities with no domain knowledge: logging setup, identifiers,
time and hashing helpers.

If something here needs to know what a card or a grade is, it belongs in
`packages/domain` instead.
