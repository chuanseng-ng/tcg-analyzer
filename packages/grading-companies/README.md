# `packages/grading-companies`

The `GradingCompanyAdapter` port and the PSA / TAG / BGS grade scales.

Grading rules are versioned and immutable — a historical analysis retains the
exact rules version it used.

Populated in M4 (#46).
