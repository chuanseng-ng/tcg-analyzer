# `database/`

PostgreSQL schema, seed data and test fixtures.

Write portable, plain-PostgreSQL DDL. Supabase is acceptable as a managed
platform, but that option only stays open if nothing depends on
Supabase-specific behaviour.
